namespace ImageMaster.App.ViewModels;

/// <summary>Which interactive canvas tool is currently active. Only one can be active at a time.</summary>
public enum ToolMode
{
    None,
    Eyedropper,
    Crop
}
